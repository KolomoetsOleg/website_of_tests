      <div class="container">
        <img src="http://placehold.it/100x30">
        <a class="btn pull-right">Выход</a> 
    </div>
 <% #render 'navigation' 
 #flash.each do |name, msg| 
 #content_tag :div, msg, :id => "flash_#{name}" if msg.is_a?(String) %>
	 