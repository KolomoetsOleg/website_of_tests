%table{:align => "", :border => "5", :cellpadding => "0", :cellspacing => "0", :width => "100pixels"}
  %tbody
    %tr
      - page = 0  #Ужасные костыли
      - session[:quest_id].map { |quests|
      - puts quests
      - if quests.has_value?(0)
        %td= link_to "-", :action => :testing, :id => page
      - else
        %td= link_to "+", :action => :testing, :id => page
      - page+=1 }