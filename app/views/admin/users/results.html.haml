= link_to('Назад', admin_users_path, :class => "btn pull-right")
%h3 Результаты пользователя "#{@user.email}"
.table.table-hover
  %table
    %tr
      %th
        %h4 Тест
      %th
        %h4 Количество балов
      %th
        %h4 Ответы лучшего результата
    - @results.each do |result|
      %tr
        - if Test.where(:id=> result.test_id).present?
          %td
            - @test = Test.find(result.test_id)
            = @test.title
          %td
            = result.bal
          %td
            = link_to "Просмотр", answer_result_admin_user_path(:t_id=>result.test_id), :class=>"btn btn-info btn-small"